<?xml version="1.0" encoding="utf-8"?>
<!--
  Toolbar surface with a hairline along its bottom edge. Applied as a background rather
  than repeating a divider View in all seven toolbar layouts, and preferred over elevation
  so the rule is equally visible in dark mode, where a shadow all but disappears.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="?attr/colorOutlineVariant" />
        </shape>
    </item>
    <item android:bottom="@dimen/divider_height">
        <shape android:shape="rectangle">
            <solid android:color="?attr/colorSurface" />
        </shape>
    </item>
</layer-list>
